# Supabase PostgreSQL Database Connection (Session Pooler - IPv4)
# Values can be supplied via environment variables. Defaults below match the current setup.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.qvhpfepyskjhvyvcbljb}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Relacion123}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=${JWT_SECRET:snackdash_super_secret_key_for_jwt_authentication_please_change_in_production_environment_for_security_purposes_always_use_strong_keys}